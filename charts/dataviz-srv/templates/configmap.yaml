apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dataviz-srv.fullname" . }}-config
  labels:
    {{- include "dataviz-srv.labels" . | nindent 4 }}
data:
  NODE_ENV: {{ .Values.env.NODE_ENV | quote }}
  PORT: {{ .Values.env.PORT | quote }}
  LOG_LEVEL: {{ .Values.env.LOG_LEVEL | quote }}